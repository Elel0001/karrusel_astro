---
import Button from "../components/Button.astro";

const { heading, text, button } = Astro.props;
---

<article>
  <div>
    <h4>{heading}</h4>
    <slot />
  </div>
  <Button type="secondary" link="">{button}</Button>
</article>

<style>
  h4 {
    padding-bottom: var(--spacing_4);
  }
  article {
    display: flex;
    flex-direction: column;
    gap: var(--spacing_8);
    align-items: center;
  }
</style>
