---
const { variant, link, page } = Astro.props;
---

<a href={link} class={variant + " " + page}><slot /></a>

<style>
  .main {
    font-size: 1.25rem;
    text-transform: uppercase;
  }
  .active {
    text-decoration: underline;
    filter: drop-shadow(0 0 4px var(--primary_50));
  }

  .breadcrumb {
    font-size: 1rem;
  }

  @media (min-width: 800px) {
    .breadcrumb {
      font-size: 1.25rem;
    }
  }
</style>
